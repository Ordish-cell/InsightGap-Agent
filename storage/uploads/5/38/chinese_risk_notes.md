# 中文混合检索说明

## 背景

这是一份用于测试中文 BM25 和 Qdrant sparse 检索的合成文档。
文档关键词包括：混合检索、稀疏向量、父子片段、回退策略。

## 关键风险

关键风险是：如果只完成部分重新摄入，稀疏向量可能缺失，导致 qdrant_hybrid 触发回退。
另一个风险是中文查询分词不稳定，需要使用命中率报告判断效果。

## 建议

先在 staging 中运行自动评估，再决定是否把 qdrant_hybrid 设为默认。
