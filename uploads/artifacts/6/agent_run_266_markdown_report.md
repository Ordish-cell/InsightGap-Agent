# 研究摘要

Based on 5 evidence items, the research question '这文档是啥？' points to a traceable information-gap opportunity. Key evidence: ### 1. 这个点一句话到底在讲什么

这个点讲的是：模型回答文档问题前，先去资料库里查相关内容。
我没有只用一种查法，而是同时用“语义相似”和“关键词命中”两种方式查。
这样短问题、章节标题、专有名词这类场景更稳。 ### 1. 这个点一句话到底在讲什么

这个点讲的是：我把多个 AI 业务能力统一接到一个总入口里。
不管用户是问文档、做旅行规划、打车、面试辅助，还是普通聊天，后端都先走一套统一流程。
这样新增 Agent 不需要到处改 Controller，也方便统一做记录、记忆、流式返回和后续编排。 ### 2. 没有这个设计之前会有什么问题

如果文档问答只用向量检索，会有一些典型问题：

1. **短 Query 容易飘**
   比如用户只问“项目背景”“接口限制”“费用规则”，向量信息太少，可能召回到语义相近但不准确的段落。

2. **章节标题不一定语义最像**
   标题很短，但它可能是用户最想找的内容。纯向量可能不如关键词直接命中。

3. **专有名词容易漏**
   比如系统名、类名、工具名、业务名，关键词检索反而更可靠。

4. **召回太多会撑爆 Prom

## 关键发现


- {'title': 'Finding from rag', 'detail': '### 1. 这个点一句话到底在讲什么\n\n这个点讲的是：模型回答文档问题前，先去资料库里查相关内容。\n我没有只用一种查法，而是同时用“语义相似”和“关键词命中”两种方式查。\n这样短问题、章节标题、专有名词这类场景更稳。', 'confidence': 0.30555555, 'evidence_refs': ['AgentHub_多Agent编排平台_面试讲解融合版.md']}

- {'title': 'Finding from rag', 'detail': '### 1. 这个点一句话到底在讲什么\n\n这个点讲的是：我把多个 AI 业务能力统一接到一个总入口里。\n不管用户是问文档、做旅行规划、打车、面试辅助，还是普通聊天，后端都先走一套统一流程。\n这样新增 Agent 不需要到处改 Controller，也方便统一做记录、记忆、流式返回和后续编排。', 'confidence': 0.25882354, 'evidence_refs': ['AgentHub_多Agent编排平台_面试讲解融合版.md']}

- {'title': 'Finding from rag', 'detail': '### 2. 没有这个设计之前会有什么问题\n\n如果文档问答只用向量检索，会有一些典型问题：\n\n1. **短 Query 容易飘**\n   比如用户只问“项目背景”“接口限制”“费用规则”，向量信息太少，可能召回到语义相近但不准确的段落。\n\n2. **章节标题不一定语义最像**\n   标题很短，但它可能是用户最想找的内容。纯向量可能不如关键词直接命中。\n\n3. **专有名词容易漏**\n   比如系统名、类名、工具名、业务名，关键词检索反而更可靠。\n\n4. **召回太多会撑爆 Prompt**\n   查到内容后还要控制长度，不然模型输入太长，成本高，也可能影响回答质量。', 'confidence': 0.17619048, 'evidence_refs': ['AgentHub_多Agent编排平台_面试讲解融合版.md']}

- {'title': 'Finding from rag', 'detail': '## 0. 阅读定位\n\n这份文档不是单纯的架构设计文档，而是给 Java 后端面试使用的项目讲解稿。\n\n你可以按下面方式使用：\n\n| 场景 | 看哪一部分 | 目标 |\n|---|---|---|\n| 想快速理解项目 | 项目整体定位 + 四个大点的一句话解释 | 先知道项目到底解决什么问题 |\n| 想背面试话术 | 每个大点的 30 秒 / 1 分钟 / 3 分钟版 | 面试时能顺畅说出来 |\n| 想应对追问 | 代码级深挖补充 + 面试官追问 | 被问到细节时不慌 |\n| 想避免吹过头 | 哪些地方不能夸大 + 防翻车话术 | 保证表达真实可信 |\n\n下面报告基于当前仓库真实代码整理。为避', 'confidence': 0.6, 'evidence_refs': ['AgentHub_多Agent编排平台_面试讲解融合版.md']}

- {'title': 'Finding from rag', 'detail': '#### 3 分钟版\n\n> 文档问答里，模型回答前一定要先查资料。最简单的做法是只用向量检索，但我在测试短 Query 和标题类问题时发现不够稳。比如用户问“接口限制”“项目背景”这种词很短，向量召回有时候会漂；但关键词检索能直接命中文档里的标题或专有名词。\n>\n> 所以我做了混合检索。入口是 RetrievalOrchestrator，它会组装检索上下文，比如 topK、相似度阈值、docType 和 sessionId。然后 VectorSearchService 走 RedisVectorStore，KeywordSearchService 走 RedisSearch。\n>\n> 两路结果', 'confidence': 0.5555556, 'evidence_refs': ['AgentHub_多Agent编排平台_面试讲解融合版.md']}

# 知识库检索

以下是从当前上传文档中检索到的相关内容，请基于这些内容回答：

[宁夏大学实验报告  (1).docx #p-0001-c-001] 宁夏大学信息工程学院
本科生实验报告
实验课程：
实验项目：
实验地点：
班    级： 软件工程一班
姓    名： 常耀嘉
学    号： 12023242424
指导教师：
实验日期：
一、实验目的：
二、实验环境：
三、实验步骤（硬件类：任务、电路图或原理框图、操作过程等；软件类：任务、流程图、关键代码与注释、主要环节等）
四、实验结果与问题分析（简述实验结果，并说明实验过程中出现的问题和解决方法，或指出本实验的局限。）
五、教师评议           成绩或评语：                           指导教师签名：                                                 年    月    日